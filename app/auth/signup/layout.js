export const metadata = {
  title: "Better Auth - Sign Up",
  description: "Create your account in Better Auth",
};

export default function SignUpLayout({ children }) {
  return (
    <div>
        {children}
    </div>
  );
}
