export const metadata = {
  title: "Better Auth - Sign In",
  description: "Sign in to Better Auth",
};

export default function SignInLayout({ children }) {
  return (
    <div>
        {children}
    </div>
  );
}
